# Factory Scope-Complete Test Architecture

“Scope-complete” is bounded to the versioned finite model: requirements, roles, states, valid and invalid transitions, policy branches, API status classes, validation codes, architecture invariants, risks, controls and evidence links. It is not an assertion about unknown future conditions. Trace paths must exist and executable collection must be nonzero.
