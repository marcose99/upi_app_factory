# Phase 46T — Repository-Native Lifecycle Maintenance

Adds `bin/upi-app-factory-lifecycle supersede-run` and `repair-resume`. Supersession preserves evidence; repair resume allows only catalogued repairs and exact commit/merge/push approval.
