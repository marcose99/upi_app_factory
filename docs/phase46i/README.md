# Phase 46I — Bounded technical namespace compatibility

Phase 46I establishes `upi_app_factory` as the canonical technical identifier
for new writes while retaining the pre-canonical technical identifier through a governed encoded read-compatible alias.

This phase does not rename Python packages, the local checkout, or the remote
repository. It does not remove the legacy alias or rewrite historical evidence.
Those operations remain explicit human-controlled migration boundaries.
