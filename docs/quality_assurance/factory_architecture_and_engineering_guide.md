# Factory Architecture and Engineering Guide

The assurance kernel accepts sealed raw measures, claim/evidence records, and a versioned finite model. Canonical JSON is authoritative; HTML, Markdown, and CSV are deterministic views. Extend report content through canonical sections, never presentation-only statements. Generated applications remain local/mock-only with no runtime LLM or real payment authority.
